// =============================================================================
// 🧠 CREATE A USERS DATABASE WITH A USERS TABLE
// =============================================================================

// command

/*
 * CREATE TABLE users (
 *   id UUID PRIMARY KEY,
 *   email TEXT NOT NULL,
 *   age INTEGER,
 *   balance NUMERIC(10,2),
 *   is_active BOOLEAN DEFAULT true,
 *   created_at TIMESTAMPTZ DEFAULT now()
 * );
 */
